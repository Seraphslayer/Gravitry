# Copy this to .env and fill in your real MongoDB Atlas connection string.
# Get it from: Atlas dashboard → Database → Connect → Drivers → copy the URI
# It looks like: mongodb+srv://<user>:<password>@cluster0.xxxxx.mongodb.net/
MONGODB_URI=
